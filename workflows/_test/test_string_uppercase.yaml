name: "Test string.uppercase"
description: "Verify string uppercase conversion"

steps:
  - id: test_basic
    module: string.uppercase
    params:
      text: "hello world"

  - id: test_mixed_case
    module: string.uppercase
    params:
      text: "HeLLo WoRLd"

  - id: test_already_upper
    module: string.uppercase
    params:
      text: "HELLO WORLD"
