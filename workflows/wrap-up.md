# Wrap-up

Before finishing work here:

- Confirm no runtime or credentials were added.
- Confirm git status is clean or handoff lists remaining files.
- Update `CHANGELOG.md` for user-visible repo-memory changes.
- Add or update a handoff entry when ownership context changes.
